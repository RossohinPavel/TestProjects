import Data.Book_lib.add
import Data.Book_lib.change
import Data.Book_lib.delete


def show_add_window(root): add.add_wind(root)


def show_change_window(root): change.change_wind(root)


def show_delete_window(root): delete.delete_wind(root)
